# Vitest resolves @vitest/coverage-v8 from the app root; without hoisting, a
# dependency only on @repo/vitest-presets stays hidden under the preset package.
# public-hoist-pattern[]=@vitest/coverage-v8
