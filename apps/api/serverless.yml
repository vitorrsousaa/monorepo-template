# "org" ensures this Service is used with the correct Serverless Framework Access Key.
org: vitorrsousaa
# "service" is the name of this project. This will also be added to your AWS resource names.
service: LifeOS
useDotenv: true

package:
  individually: true

plugins:
  - serverless-dotenv-plugin

provider:
  name: aws
  runtime: nodejs22.x
  stage: ${opt:stage, 'dev'}
  region: us-east-1
  deploymentMethod: direct
  architecture: arm64
  memorySize: 128
  environment:
    STAGE: ${self:provider.stage}
    COGNITO_CLIENT_ID: !Ref UserPoolClient
  httpApi:
    authorizers:
      CognitoAuthorizer:
        type: jwt
        identitySource: $request.header.Authorization
        issuerUrl: !GetAtt UserPool.ProviderURL
        audience: !Ref UserPoolClient
    cors:
      allowedOrigins:
        - http://localhost:5173
      allowedHeaders:
        - Content-Type
        - Authorization
      allowedMethods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
  iam:
    role:
      statements:
        - Effect: "Allow"
          Action:
            - "cognito-idp:AdminGetUser"
          Resource: !GetAtt UserPool.Arn
        - Effect: "Allow"
          Action:
            - dynamodb:PutItem
            - dynamodb:DeleteItem
            - dynamodb:GetItem
            - dynamodb:UpdateItem
            - dynamodb:Scan
            - dynamodb:Query
            - dynamodb:ConditionCheckItem
            - dynamodb:BatchWriteItem
          Resource: 
            - !GetAtt DatabaseTable.Arn
            - !Sub ${DatabaseTable.Arn}/index/GSI1

custom:
  userPoolName: ${self:service}-${self:provider.stage}-UserPool
  userPoolDomain: lifeos-${self:provider.stage}-userpool
  dotenv:
    exclude:
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_SESSION_TOKEN

build:
  esbuild:
    bundle: true
    minify: true
    sourcemap: false
    exclude:
      - "@aws-sdk/*"
    target: node20
    
resources:
  - ${file(./serverless/resources/UserPool.yml)}
  - ${file(./serverless/resources/Database.yml)}

functions:
  signup:
    handler: src/server/functions/auth/signup/index.handler
    events:
      - httpApi:
          path: /auth/signup
          method: post
  signin:
    handler: src/server/functions/auth/signin/index.handler
    events:
      - httpApi:
          path: /auth/signin
          method: post
  getAccountInfo:
    handler: src/server/functions/auth/account/index.handler
    events:
      - httpApi:
          path: /auth/account-info
          method: get
          authorizer: 
            name: CognitoAuthorizer
  getUserSettingsByUserId:
    handler: src/server/functions/settings/get-by-user-settings/handler
    events:
      - httpApi:
          path: /settings/user-settings
          method: get
          authorizer: 
            name: CognitoAuthorizer
  preSignUpTrigger:
    handler: src/server/triggers/auth/presignup/index.handler
    events:
      - cognitoUserPool:
          pool: ${self:custom.userPoolName}
          trigger: PreSignUp
          existing: true
          forceDeploy: true
  getTodos:
    handler: src/server/functions/todo/get-todos/index.handler
    events:
      - httpApi:
          path: /todos
          method: get
          authorizer: 
            name: CognitoAuthorizer
  getInboxTasks:
    handler: src/server/functions/tasks/get-inbox-tasks/index.handler
    events:
      - httpApi:
          path: /tasks/inbox
          method: get
          authorizer: 
            name: CognitoAuthorizer
  getTodayTasks:
    handler: src/server/functions/tasks/get-today-tasks/index.handler
    events:
      - httpApi:
          path: /tasks/today
          method: get
          authorizer: 
            name: CognitoAuthorizer
  getDashboardAnalytics:
    handler: src/server/functions/tasks/get-dashboard-analytics/index.handler
    events:
      - httpApi:
          path: /tasks/dashboard
          method: get
          authorizer: 
            name: CognitoAuthorizer
  getAllProjectsByUser:
    handler: src/server/functions/projects/get-all-projects-by-user/index.handler
    events:
      - httpApi:
          path: /projects
          method: get
          authorizer: 
            name: CognitoAuthorizer
  createProject:
    handler: src/server/functions/projects/create-project/index.handler
    events:
      - httpApi:
          path: /projects
          method: post
          authorizer: 
            name: CognitoAuthorizer
  getProjectDetail:
    handler: src/server/functions/projects/get-project-detail/index.handler
    events:
      - httpApi:
          path: /projects/{projectId}/detail
          method: get
          authorizer: 
            name: CognitoAuthorizer
  getProjectsSummary:
    handler: src/server/functions/projects/get-projects-summary/index.handler
    events:
      - httpApi:
          path: /projects/summary
          method: get
          authorizer:
            name: CognitoAuthorizer
  createSection:
    handler: src/server/functions/sections/create-section/index.handler
    events:
      - httpApi:
          path: /projects/{projectId}/sections
          method: post
          authorizer: 
            name: CognitoAuthorizer
  getAllSectionsByProject:
    handler: src/server/functions/sections/get-all-by-project/index.handler
    events:
      - httpApi:
          path: /projects/{projectId}/sections
          method: get
          authorizer: 
            name: CognitoAuthorizer
  createTasks:
    handler: src/server/functions/tasks/create-tasks/index.handler
    events:
      - httpApi:
          path: /tasks/create
          method: post
          authorizer:
            name: CognitoAuthorizer
  updateTaskCompletion:
    handler: src/server/functions/tasks/update-task-completion/index.handler
    events:
      - httpApi:
          path: /tasks/{taskId}/completion
          method: patch
          authorizer:
            name: CognitoAuthorizer
  updateTask:
    handler: src/server/functions/tasks/update-task/index.handler
    events:
      - httpApi:
          path: /tasks/{taskId}
          method: put
          authorizer:
            name: CognitoAuthorizer