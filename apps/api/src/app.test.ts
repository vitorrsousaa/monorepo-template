import { describe, it, expect } from "vitest";

describe("API App", () => {
	it("should pass a simple test", () => {
		expect(1 + 1).toBe(2);
	});

	it("should handle basic math", () => {
		expect(2 * 2).toBe(4);
	});
});
